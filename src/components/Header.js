// Header

import { Link } from 'react-router-dom';
import Nav from './Nav';
import { appTitle } from '../globals/globals';

const Header = () => {

    return (
        <header>
            <Nav />
        </header>
    );
}

export default Header;